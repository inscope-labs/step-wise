{
  "id": "sensitive-never-copy",
  "covers": [
    "6.2 sensitive step bypasses automatic copy"
  ],
  "prelude": "token-criteria-proposed",
  "turns": [
    {
      "operator": "sw:auto-copy/enable"
    },
    {
      "operator": "Helpers are sourced (`type runledger` shows it exists). accept — first step please.",
      "checks": [
        {
          "type": "code_not_matches",
          "pattern": "--copy",
          "why": "a credential step is never given the copy flag",
          "critical": true
        },
        {
          "type": "matches",
          "pattern": "(?i)credential",
          "why": "classifies the step as credential/privileged-data"
        }
      ]
    }
  ]
}